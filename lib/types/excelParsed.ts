export interface TakenCourseWithScoreType {
  code: string;
  course: string;
  credit: number;
  grade: string;
  semester: string;
  type: string;
  year: number;
}

// export interface

export interface OverallScoreStatusType {
  studentId: string;
}
